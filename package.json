{
  "name": "esame-sesto-senso",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "start": "vite",
    "dev": "vite",
    "build": "vite build",
    "build:pages": "vite build",
    "preview": "vite preview",
    "server": "npm --prefix server run dev",
    "server:start": "npm --prefix server start"
  },
  "dependencies": {
    "lil-gui": "^0.21.0",
    "qrcode": "^1.5.4",
    "three": "^0.176.0"
  },
  "devDependencies": {
    "vite": "^6.0.0"
  }
}
